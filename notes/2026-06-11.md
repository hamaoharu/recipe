# 2026-06-11

- setState関数はprevで現在のstateを受け取れる

- 配列メソッドは配列がない場合を考慮して空配列になる処理を用意
- some コールバック関数の条件が一致するかどうか 　booleanで返す filterは配列で返す
- 元要素に変更を加えないようスプレッドでコピー

```
list = list.filter(
    (r) =>
        r.title.toLowerCase().includes(lower) ||
        r.description.toLowerCase().includes(lower) ||
        (r.tags ?? []).some((t) => t.toLowerCase().includes(lower))
);
```

- 再レンダーのたびに実行されるのでuseEffect必要ない

```  
const q = searchParams.get("q") ?? "";
const tagFilter = searchParams.get("tag") ?? "";
```

- URLSearchParamsはクエリ以降に変更可能 useSearchParams()はフックで読み取り専用

```
const params = new URLSearchParams(searchParams.toString());
    if (params.get("tag") === tag) {
      params.delete("tag");
    } else {
      params.set("tag", tag);
    }
    const query = params.toString();
    router.push(query ? `/?${query}` : "/");
```