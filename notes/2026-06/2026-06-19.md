# 2026-06-19

- ?.以前がundefinedやnullの時そこで止めてundefinedを返す .authorでエラーになるのを防ぐ
```
  const author = userRoadmaps[0]?.author ??
```

- [id]部分をparamsとして受け取れる(Next.js)
```
export default function UserPage({ params })
```

- キーと値の型が決まったオブジェクト Record
```
useState<Record<string, boolean>>({})
```