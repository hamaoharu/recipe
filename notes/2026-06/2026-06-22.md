# 2026-06-22

- header.jsのTS化
- React19ではFormEventは使わない フォームのonSubmitには、SubmitEvent が推奨
- SubmitEvent 名前がブラウザの型と同じなので React.から記述して区別
```
  const handleSearch = (e: React.SubmitEvent<HTMLFormElement>) =>
```