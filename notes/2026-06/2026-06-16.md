# 2026-06-16

- ビルドツールはファイルをブラウザが読めるように変換するもの
- viteはビルドツール Next.jsはフレームワークだが、ビルドツールが組み込まれている
- tsの基本的な型定義

- 決まった文字列しか入らないようにする型エイリアス
```
type SortMode = "new" | "trend";
```
- stateはジェネリクス型で指定
- 変数がキーの時はRecord<キー、値>の型を使用するといい
```
const [liked, setLiked] = useState<Record<string, boolean>>({});
```

- localStrageからとってきたデータがRoadmap[]型であることを保証する 型アサーション(as)
```
const saved = JSON.parse(localStorage.getItem("user_roadmaps") ?? "[]") as Roadmap[];
```

- イベントオブジェクトにも型定義
- 型のインポートも忘れずに
```
import type { MouseEvent } from "react";

const toggleBookmark = (e: MouseEvent<HTMLButtonElement>, id: string) =>
```

- as constはvalueの型がstringになることを防ぐ 文字列リテラル型としてそのまま扱うことを明示
```
[
{ value: "new", label: "新着" },
{ value: "trend", label: "トレンド" },
] as const
```

- 関数の型だけ定義するときの書き方は違う
- propsはオブジェクトの後ろでまとめて型定義
```
onClick: (id: string) => void;

function NodeBox({ node, selected, onClick }: NodeBoxProps)
```