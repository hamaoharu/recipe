# 2026-07-02

- API プログラム同士がデータをやり取りするための窓口
- API ROUTES Next.jsでAPIを作成する仕組み
- app/api/health/route.ts サーバー生存確認で慣習的に/health
- DBに接続するためにAPI構築が必要

- APIがGETメソッドでロードマップ情報を取得するための関数を用意
```
export async function GET() {
    return Response.json(ROADMAPS);
}
```

- page.tsxからfetchで取得
- メソッドを書かないと自動でGETになる
```
fetch("/api/roadmaps", {method: "GET"})
    //JSONに変換
    .then((res) => res.json())
    //stateに保存
    .then((data: Roadmap[]) => setRoadmaps(data))
    //エラーが出た時に実行
    .catch(() => {});
```

- fetch or async