# 2026-07-03

- Route Handler HTTPリクエストを受け取って返事を返す関数 GET POSTとか
- 引数の順番と形はNext.jsが決めている

```
export async function POST (
    //リクエスト
    request: Request,
    //URLの[id]部分
    {params}: {params: Promise<{id: string}>}
)
```

- curl ターミナルからHTTPリクエストを送るコマンド fetchと同じこと
```
curl リンク (GET)
curl -X POST リンク (POST)
```